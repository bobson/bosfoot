---
// Redirect root URL to default locale.
// Astro's i18n config has redirectToDefaultLocale: true, which handles this
// at build/runtime. This file is a fallback in case routing changes later.
return Astro.redirect('/mk/')
---